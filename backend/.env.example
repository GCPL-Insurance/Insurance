# ── Supabase ──────────────────────────────────────────────────────────────────
SUPABASE_URL=https://xnqxbtgymnwdmzxzgqdt.supabase.co

# ⚠️  MUST be the SERVICE ROLE key — NOT the anon key
# Get it from: Supabase Dashboard → Project Settings → API → service_role (secret)
# The backend validates this on startup — it will refuse to start with an anon key.
SUPABASE_SERVICE_ROLE_KEY=sb_secret_lAAH8xLS1qAdJ9p22wrYLQ_9Wi6e3XD

# ── CORS ──────────────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=https://gcpl.insurance-portal.in,https://enroll.insurance-portal.in,http://localhost:5173

# ── Server ────────────────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=production

# ── Cloudflare Turnstile ──────────────────────────────────────────────────────
# Get from: Cloudflare Dashboard → Turnstile → your site → Secret key
# If not set, captcha is skipped in development (NODE_ENV != production)
TURNSTILE_SECRET_KEY=0x4AAAAAACkZLYXFjh9JLqraVB3rcEOGC9Q
